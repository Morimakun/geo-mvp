# Phase 6 新SFA報告用紙対応計画

**日時：** 2026-06-21（補正版）  
**対象：** 新SFA報告用紙修正20260621完成.pdf  
**方針：** 既存照合ロジックを維持しながら、新項目抽出を追加

---

## 1. 新帳票レイアウト確認

### 帳票上部に新規追加される項目

```
【帳票区分】 1 = 店舗  / 2 = イベント
【提出区分】 1 = 初回  / 2 = 再送（変更あり）/ 3 = 再送（変更なし）
【基本情報】 スタッフID / スタッフ名 / 店舗コード / 店舗名 / 日付
【識別子】   右下：日報データNo. / タブレットNo.
```

---

## 2. 内部定義（変換マッピング）

| 帳票表示 | 内部値 | 意味 |
|---------|--------|------|
| **帳票区分** |  |  |
| 1 店舗 | `store` | 通常の店舗営業 |
| 2 イベント | `event` | イベント営業 |
| **提出区分** |  |  |
| 1 初回 | `initial` | 最初の報告 |
| 2 再送（変更あり） | `resend_with_changes` | 修正・差替相当 |
| 3 再送（変更なし） | `resend_without_changes` | 再送のみ（値変わらず） |

---

## 3. 抽出対象項目の拡張

### 現在の抽出項目（extractor.py）
```
- date（日付）
- store（店舗名）
- name（氏名）
- data_no（日報データNo）
- tab_no（タブレットNo）
- 合計欄（total_left, total_right）
```

### 新規追加項目
```
- activity_type_raw（帳票区分 生値：1/2）
- activity_type（帳票区分 変換値：store/event）
- activity_type_label（帳票区分 表示用：店舗/イベント）
- submission_type_raw（提出区分 生値：1/2/3）
- submission_type（提出区分 変換値：initial/resend_with_changes/resend_without_changes）
- submission_type_label（提出区分 表示用：初回/再送（変更あり）/再送（変更なし））
- staff_id（スタッフID）
- staff_name（スタッフ名）
- store_code（店舗コード）
- store_name（店舗名）
- daily_report_data_no（日報データNo）
- tablet_no（タブレットNo）
```

**注意：** 既存の date / name / data_no / tab_no は維持。新帳票では冗長化が起きても、既存抽出項目は残す。

---

## 4. 照合キー方針

### 基本照合キー
```
(store_code, screen_selected_business_date, activity_type)
```

つまり：
- **店舗コード** ← 帳票上部の店舗コード（CSV col281 と対応）
- **画面で選択された帳票対象営業日** ← ユーザーが画面で選択した営業日
- **帳票区分** ← 店舗/イベント

### 帳票上の日付の扱い
- **抽出対象：** はい（取得する）
- **照合キー：** いいえ（参考値として扱う）
- **表示方法：** 確認画面に参考値として表示
- **ズレ判定：** 画面選択日とズレている場合は要確認フラグを立てる
- **理由：** FAX送信日と帳票対象営業日がズレることがあり、画面選択が正とする設計

### 店舗コードの前処理
```
1. 空白除去（前後の空白を削除）
2. 全角・半角の正規化
3. 文字列として保持
4. CSV col281 と比較

例：「ＡＵ１ＫＢ８３０２２７」→ 「AU1KB830227」
```

### スタッフ識別の優先順位
1. **スタッフID** ← 新帳票で追加、最も安定
2. **スタッフ名** ← 既存の name 相当、表記ゆれの可能性あり

スタッフ名は表記ゆれが起きやすいため、照合・確認時はスタッフIDをより重視します。

### 提出区分の役割
- **照合キーには使わない**
- **同一キーで複数帳票が届いた場合の確認フラグとして使う**

---

## 5. 実装の最重要原則：「チェックボックスはヒント、値比較が真実」

### 原則定義
```
チェックボックス・番号区分は「運用者の意図を示すヒント」であり、
実際の真実は「PDF帳票と Salesforce CSV の主要数値の一致・不一致」である。
```

これは以下を意味します：

- ✅ **値が同じ → 一致**（チェックボックスに関わらず）
- ✅ **値が違う → 不一致**（チェックボックスに関わらず確認対象）
- ✅ **チェック矛盾 → 確認フラグ**（値と矛盾する場合、要確認として浮かび上がらせる）
- ❌ **チェックボックス最優先ではない**（「再送（変更なし）」でも値が違えば確認対象）

### 判定ロジック（優先度順）
```
1. 値比較：主要数値（AI合計欄、CZ合計欄等）が同じか違うか
2. チェック矛盾判定：提出区分と値の差異が矛盾していないか
3. 確認フラグ：チェック矛盾があれば「要確認」「警告」レベルを決定
4. 優先候補：後から届いた帳票を優先候補にする（値比較の上で）
5. 自動採用：絶対にしない（最終判定は確認者）
```

---

## 6. 同一キーで複数帳票がある場合の処理フロー

### Case 1: `submission_type = resend_without_changes` なのに主要数値が異なる
```
判定：要確認
理由：再送（変更なし）なのに数値差異あり
可能性：OCR誤読 / 記入差異 / 黙示的な修正
表示：差異を赤色フラグで表示、確認対象に含める
```

### Case 2: `submission_type = resend_with_changes` で主要数値が異なる
```
判定：後送分を優先候補
理由：修正・差替宣言されている
表示：旧版との差分を明示、確認対象として表示
自動確定：しない（確認者が最終判定）
```

### Case 3: `submission_type = resend_with_changes` なのに主要数値が同じ
```
判定：軽微（確認優先度低）
理由：修正ありだが値変わらず
表示：情報の参考値として表示（低優先）
```

### Case 4: 提出区分が未読取・未記入で同一キーの2枚目がある
```
判定：要確認
理由：再送か修正か判断できない
表示：確認対象として表示、スタッフに問い合わせ対象
```

---

## 7. 実装対象・対象外

### 現在の段階（2026-06-21）
✅ **計画書補正完了**  
❌ **コード実装はまだしない**

理由：新帳票実物とそのCSVペアでの検証が必要

### 実装対象（将来的に）

**A. extractor.py の修正**
- 新プロンプトに帳票区分・提出区分・スタッフID・店舗コード等を追加
- ExtractionResult クラスに新項目フィールドを追加
- 値の変換マッピング実装（1 → store 等）
- 店舗コードの前処理：空白除去・全角半角正規化

**B. reconciliation.py の修正**
- reconcile() 関数の照合キー：`(store_code, screen_selected_business_date, activity_type)` に更新
- 帳票上の日付は参考値のみ（キー対象外）
- Case 1-4 の判定ロジックを追加
- **重要：既存の数値比較ロジックは変更しない**
- 値比較が常に優先（チェックボックスはヒント）

**C. app.py の修正**
- 抽出結果JSON出力に新項目を含める
- 確認ログCSVに新項目を追加
- 確認画面で参考値として新項目を表示
- 帳票上の日付とズレ判定：ズレている場合は注釈を表示

**D. docs 更新**
- このファイル（既に完了）

### 実装しない（禁止事項）

- ❌ 完全自動確定ロジック
- ❌ 再送・修正分の自動統合
- ❌ Salesforce連携
- ❌ CSVへの自動反映
- ❌ 既存照合ロジックの全面改修
- ❌ 値比較より チェックボックスを優先する実装
- ❌ app.py の大規模UI改修（最小表示追加のみ）

---

## 8. 実装前チェックリスト（実装時に必須）

本実装に入る前に、以下の検証を完了させること：

- [ ] 新帳票実物でスタッフID・店舗コードが正確に読み取れるか確認
- [ ] 新帳票の店舗コード と CSV col281 の一致度を検証（サンプル帳票で）
- [ ] 帳票上の日付が、画面で選択される営業日と同じか違うか確認
- [ ] 新帳票でのチェックボックス記入形式（チェック、番号、どちらか両方）を確認
- [ ] extractor.py のプロンプトで新項目が正確に抽出できるか試験

---

## 9. スケジュール

| Phase | 内容 | 担当 | 期間 |
|-------|------|------|------|
| A | 新帳票実物確認、不確定要素の解消 | 将大 | 2026-06-21～06-25 |
| B | extractor.py / reconciliation.py 更新 | Claude Code | 2026-06-25～06-28 |
| C | app.py 更新・テスト | Claude Code | 2026-06-28～06-29 |
| D | 新帳票での動作確認 | 将大 | 2026-06-29～07-02 |

---

## 10. 参考：既存コード構造

```
geo-mvp/
├── extractor.py          ← PDF抽出ロジック
├── reconciliation.py      ← CSV照合ロジック
├── reconciliation_phase1.py ← Phase 1特化版
├── app.py                ← Streamlit UI
└── docs/
    └── PHASE_6_NEW_SFA_FORM_SUPPORT_PLAN.md (このファイル)
```

---

**作成日：** 2026-06-21  
**補正日：** 2026-06-21  
**ステータス：** 計画書完成 / 本実装待ち
